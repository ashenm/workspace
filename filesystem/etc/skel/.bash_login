. $HOME/.profile

if [ $(id -u) -ne 0 ]; then
  export GIT_PS1_SHOWDIRTYSTATE=1
  export PS1="\$(__git_ps1 \"(%s) \")$PS1"
fi

if [ -f /home/ubuntu/.travis/travis.sh ]; then
  . $(dirname $(dirname $(gem which travis)))/assets/travis.sh
fi

# vim: set expandtab shiftwidth=2 syntax=sh:
