[GLOBAL]
pants_version = "2.15.0"
backend_packages = [
  "pants.backend.docker",
  "pants.backend.docker.lint.hadolint",
]

[anonymous-telemetry]
enabled = false

[docker]
default_repository = "ashenm/workspace"

[docker.registries.hub]
address = "registry.hub.docker.com"
