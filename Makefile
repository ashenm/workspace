build:
	./scripts/build.sh

assess:
	./scripts/assess.sh

clean:
	./scripts/clean.sh

culminate:
	./scripts/culminate.sh

deploy:
	./scripts/deploy.sh

install:
	./scripts/install.sh

purge:
	./scripts/clean.sh --all

run:
	./scripts/run.sh --dev --dry
